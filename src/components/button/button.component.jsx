cont CustomButton = ({ childern, ...otherProps}) => (
    <button 
    className = 
    {
        'custom-button'}
        {...otherProps}
        >
        {children}
        </button>
    
)

export default CustomButton